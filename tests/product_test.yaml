folder: Product API Tests
tests:
  - name: Get Product
    # enabled: false
    request:
      method: get
      path: /product
      query:
        id: 11

    assertions:
      - type: status
        equals: 200

      - type: json
        path: success
        equals: true

      - type: json
        path: product_id
        equals: 11